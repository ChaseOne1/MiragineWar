#pragma once

namespace app {
// NOTE: these definition mean functional key/button, different function can be mapped to same key

enum class Key : uint8_t
{
    MOUSE_SELECT,
    CAM_MOVE_UP,
    CAM_MOVE_DOWN,
    CAM_MOVE_LEFT,
    CAM_MOVE_RIGHT,
    KEY_COUNT
};

enum class Button
{
};
}
